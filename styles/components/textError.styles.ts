import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  errorText: {
    marginTop: 5,
    color: "#E33C3C",
    fontSize: 12,
    fontFamily: "Quicksand",
    fontWeight: '400'
  },
});